---
import Analytics from "../components/Analytics.astro";
---

<html lang="tr">
  <head>
    <meta charset="utf-8" />
    <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <meta name="theme-color" content="#000000" />
    <meta name="description" content="Steam'in Türkçesi" />
    <meta name="keywords" content="steam, türkçe, oyun, video oyun, oyun platformu" />
    <meta name="author" content="BuharTR" />
    <meta name="robots" content="all" />
    <link rel="sitemap" href="/sitemap.xml" />
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <title>BuharTR - Steam'in Türkçesi</title>
    <style>
      body {
        background-color: black;
        height: 100vh;
        margin: 0;
        position: relative;
        user-select: none;
        pointer-events: none;
      }

      #logo, #sixteen {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      }

      #logo {
        width: 400px;
        max-width: 65vw;
        height: auto;
      }

      #sixteen {
        color: white;
        font-size: min(5vw, 2rem);
        font-family: "Courier New", Courier, monospace;
        line-height: 1;
        animation: blink 1s infinite;
      }

      @keyframes blink {
        0% {
          opacity: 1;
        }
        50% {
          opacity: 0.5;
        }
        100% {
          opacity: 0.5;
        }
      }
    </style>
    <script>
      const sixteen = document.getElementById("sixteen");

      const countDownDate = new Date("Jan 1, 2025 00:00:00").getTime();

      const updateCountdown = () => {
        const distance = countDownDate - new Date().getTime();
        const totalSeconds = Math.max(0, Math.floor(distance / 1000));

        if (sixteen) {
          sixteen.textContent = totalSeconds.toString().padStart(7, "0");
        }

        if (distance < 0) {
          clearInterval(intervalId);
        }
      };

      const intervalId = setInterval(updateCountdown, 1000);

      updateCountdown();
    </script>
    <Analytics />
  </head>
  <body>
    <img id="logo" src="/buhartr.png" alt="BuharTR Logosu" />
    <span id="sixteen" />
  </body>
</html>
